[tox]
envlist = py27

[testenv]
changedir = {envsitepackagesdir}
deps =
    setuptools >= 8.0
    pytest
    pytest-cov
    pytest-flakes
    pytest-pep8
    pyquery
    mock
    webtest
commands =
    py.test {posargs}
