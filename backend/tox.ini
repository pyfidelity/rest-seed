[tox]
envlist = py27

[testenv]
changedir = {envsitepackagesdir}
deps =
    pip >= 6.1.1
    setuptools >= 8.0
    pytest
    pytest-cov
    pytest-flakes
    pytest-pep8
    pyquery
    mock
    webtest
commands =
    py.test {posargs}
